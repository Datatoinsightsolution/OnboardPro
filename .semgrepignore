# Semgrep ignore patterns — https://semgrep.dev/docs/ignoring-files-folders-code
#
# NOTE: the presence of this file REPLACES semgrep's built-in default ignore list,
# so the usual defaults are restated below rather than inherited.

# ── Build output ──────────────────────────────────────────────────────────────
# Generated by `cd frontend && yarn build` and committed so the app can be installed
# without a node toolchain. It is minified third-party code (frappe-ui, tiptap, Vue).
# Findings here are not actionable: they cannot be fixed in this repo's source, and
# every rebuild reshuffles the bundle hashes, so they would reappear indefinitely.
onboardpro/public/frontend/
onboardpro/public/dist/
*.min.js

# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/
vendor/
.venv/
venv/
env/

# ── Tooling / VCS ─────────────────────────────────────────────────────────────
.git/
.github/
.semgrep/
.semgrep_logs/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
