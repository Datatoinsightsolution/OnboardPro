# Build output — generated by `cd frontend && yarn build`, committed so the app can be
# installed without a node toolchain. Never hand-edit or format these: prettier would
# expand the minified vendor bundle, and the next build would revert it, so CI would
# fail on every rebuild forever.
onboardpro/public/

node_modules/
